import { JiraContentsExcel } from './jira-contents-excel';

describe('JiraContentsExcel', () => {
  it('should create an instance', () => {
    expect(new JiraContentsExcel()).toBeTruthy();
  });
});
