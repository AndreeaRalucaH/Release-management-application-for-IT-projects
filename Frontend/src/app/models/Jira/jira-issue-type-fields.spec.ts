import { JiraIssueTypeFields } from './jira-issue-type-fields';

describe('JiraIssueTypeFields', () => {
  it('should create an instance', () => {
    expect(new JiraIssueTypeFields()).toBeTruthy();
  });
});
